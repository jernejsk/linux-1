# SPDX-License-Identifier: (GPL-2.0-only OR BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/display/allwinner,sun50i-h6-de3-wb.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Allwinner DE3 and DE33 Writeback Engine

maintainers:
  - Jernej Skrabec <jernej.skrabec@gmail.com>

properties:
  compatible:
    enum:
      - allwinner,sun50i-h6-de3-wb
      - allwinner,sun50i-h616-de33-wb

  reg:
    minItems: 1
    maxItems: 2

  reg-names:
    items:
      - const: writeback
      - const: rcq

  clocks:
    items:
      - description: Bus clock
      - description: Module clock

  clock-names:
    items:
      - const: bus
      - const: mod

  iommus:
    maxItems: 1

  interrupts:
    maxItems: 1

  resets:
    maxItems: 1

  allwinner,top:
    $ref: /schemas/types.yaml#/definitions/phandle
    description: Phandle to the DE33 top-level clock and routing controller

  ports:
    $ref: /schemas/graph.yaml#/properties/ports

    properties:
      port@0:
        $ref: /schemas/graph.yaml#/$defs/port-base
        description: Inputs from display engine mixers

        properties:
          endpoint@0:
            $ref: /schemas/graph.yaml#/properties/endpoint
            description: Input from mixer 0
            properties:
              reg:
                const: 0

          endpoint@1:
            $ref: /schemas/graph.yaml#/properties/endpoint
            description: Input from mixer 1
            properties:
              reg:
                const: 1

        anyOf:
          - required:
              - endpoint@0
          - required:
              - endpoint@1

        unevaluatedProperties: false

    required:
      - port@0

required:
  - compatible
  - reg
  - clocks
  - clock-names
  - interrupts
  - resets
  - ports

allOf:
  - if:
      properties:
        compatible:
          const: allwinner,sun50i-h616-de33-wb
    then:
      properties:
        reg:
          minItems: 2
        reg-names:
          minItems: 2
        iommus: false
      required:
        - reg-names
        - allwinner,top
    else:
      properties:
        reg:
          maxItems: 1
        reg-names: false
        allwinner,top: false
      required:
        - iommus

additionalProperties: false

examples:
  - |
    #include <dt-bindings/clock/sun8i-de2.h>
    #include <dt-bindings/interrupt-controller/arm-gic.h>
    #include <dt-bindings/reset/sun8i-de2.h>

    writeback@10000 {
        compatible = "allwinner,sun50i-h6-de3-wb";
        reg = <0x10000 0x1000>;
        clocks = <&display_clocks CLK_BUS_WB>,
                 <&display_clocks CLK_WB>;
        clock-names = "bus", "mod";
        resets = <&display_clocks RST_WB>;
        iommus = <&iommu 0>;
        interrupts = <GIC_SPI 78 IRQ_TYPE_LEVEL_HIGH>;

        ports {
            #address-cells = <1>;
            #size-cells = <0>;

            port@0 {
                #address-cells = <1>;
                #size-cells = <0>;
                reg = <0>;

                writeback_in_mixer0: endpoint@0 {
                    reg = <0>;
                    remote-endpoint = <&mixer0_out_writeback>;
                };

                writeback_in_mixer1: endpoint@1 {
                    reg = <1>;
                    remote-endpoint = <&mixer1_out_writeback>;
                };
            };
        };
    };

...
